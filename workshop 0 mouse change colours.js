let x=0;

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  
  if(mouseX>width/2){
    fill(255,0,0);
  }else {
  fill(255,255,0)}
 
  rect(50,100,40,60)
  
//  x=x+3;
  
//  if(x > width  )
//   {x=0}
}